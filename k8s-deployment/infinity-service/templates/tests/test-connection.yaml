apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infinity-service.fullname" . }}-test-connection"
  labels:
    {{- include "infinity-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infinity-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "infinity-service.fullname" . }}
  labels:
    {{- include "infinity-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infinity-service.selectorLabels" . | nindent 4 }}
